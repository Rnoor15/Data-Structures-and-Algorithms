The makefile is the created with the correct files

To compile, use make all

To clean, use make clean

Run with the proper file arguments, such as the adjacency and graph files

Everything in project is completed, and correctly implemented

If there was no path from a certain vertex to another, "No path" was outputted
Used priority queue for Dijkstra, and regular queue for TopologicalSort.
